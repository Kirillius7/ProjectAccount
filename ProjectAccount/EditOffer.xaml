<Window x:Class="ProjectAccount.EditOffer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectAccount"
        mc:Ignorable="d"
        Title="EditAnnouncment" Height="450" Width="800"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#FF5F91B7">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="111"/>
            <ColumnDefinition Width="578"/>
            <ColumnDefinition Width="111"/>
        </Grid.ColumnDefinitions>

        <Button x:Name="Apply" Content="Confirm" Command ="{Binding EditOfferCommand}" HorizontalAlignment="Left" Margin="113,363,0,0" VerticalAlignment="Top" Width="86" Height="31" Background="White" BorderBrush="Black" Grid.Column="1">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Name="buttonBorder" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="LightGray" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button x:Name="ReturnB" Content="Back" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Click="ReturnB_Click" Height="27" Width="68" Background="White" BorderBrush="Black">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Name="buttonBorder" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="LightGray" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button x:Name="search_offer" Content="Search" Command ="{Binding searchAnnouncment}" HorizontalAlignment="Center" Margin="0,170,0,0" VerticalAlignment="Top" Width="86" Height="31" Background="White" BorderBrush="Black">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Name="buttonBorder" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="LightGray" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button x:Name="filter_offer" Content="Filter" HorizontalAlignment="Left" Margin="462,350,0,0" VerticalAlignment="Top" Command ="{Binding filterAnnouncment}" Width="86" Height="31" Background="White" BorderBrush="Black" Grid.Column="1">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Name="buttonBorder" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="LightGray" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <Button x:Name="Clear" Content="Clear" HorizontalAlignment="Left" Margin="203,363,0,0" VerticalAlignment="Top" Width="86" Height="31" Click="Clear_Click" Background="White" BorderBrush="Black" Grid.Column="1">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Name="buttonBorder" Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="LightGray" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="buttonBorder" Property="Background" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <ListView x:Name="Listv" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ItemsSource="{Binding _announcments}" Margin="0,77,0,205" BorderBrush="Black" Grid.Column="1" >
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding id}" Header="ID" Width="35"/>
                    <GridViewColumn DisplayMemberBinding="{Binding nameprod}" Header="Name" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding nameprob}" Header="Problem" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding typeprob}" Header="Type problem" Width="125"/>
                    <GridViewColumn DisplayMemberBinding="{Binding state}" Header="State" Width="100"/>
                    <GridViewColumn DisplayMemberBinding="{Binding urgency}" Header="Urgency" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <ComboBox x:Name="cmbbox1" Height="24" HorizontalAlignment="Left" Margin="274,257,0,0" VerticalAlignment="Top" Text="{Binding typeprod, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" Width="158" RenderTransformOrigin="0.545,0.554" Grid.Column="1" FontSize="14">
            <ComboBoxItem Content="Electrician" />
            <ComboBoxItem Content="Plumber" />
            <ComboBoxItem Content="Programmer" />
            <ComboBoxItem Content="Welder" />
            <ComboBoxItem Content="Mechanic" />
            <ComboBoxItem Content="Carpenter" />
            <ComboBoxItem Content="Auto Mechanic" />
            <ComboBoxItem Content="HVAC Technician" />
            <ComboBoxItem Content="Locksmith" />
            <ComboBoxItem Content="Computer Technician" />
        </ComboBox>

        <ComboBox x:Name="cmbbox2" Height="24" HorizontalAlignment="Left" Margin="141,321,0,0" VerticalAlignment="Top" Text="{Binding state, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" Width="140" RenderTransformOrigin="0.701,-0.017" Grid.Column="1" FontSize="14">
            <ComboBoxItem Content="Terrible" />
            <ComboBoxItem Content="Poor" />
            <ComboBoxItem Content="Fair" />
            <ComboBoxItem Content="Relatively good" />
            <ComboBoxItem Content="Good" />
        </ComboBox>

        <ComboBox x:Name="cmbbox3" Height="24" HorizontalAlignment="Left" Margin="292,321,0,0" VerticalAlignment="Top" Text="{Binding urgency, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" Width="140" RenderTransformOrigin="0.859,-5.388" Grid.Column="1" FontSize="14">
            <ComboBoxItem Content="Immediate" />
            <ComboBoxItem Content="Expediate" />
            <ComboBoxItem Content="On the double" />
            <ComboBoxItem Content="Important" />
            <ComboBoxItem Content="Time-insensitive" />
        </ComboBox>

        <ComboBox x:Name="cmbbox4" Height="24" HorizontalAlignment="Left" Margin="445,257,0,0" VerticalAlignment="Top" Text="{Binding filter, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" Width="120" Grid.Column="1" FontSize="14">
            <ComboBoxItem Content="Name" />
            <ComboBoxItem Content="Problem" />
            <ComboBoxItem Content="Type problem" />
            <ComboBoxItem Content="State" />
            <ComboBoxItem Content="Urgency" />
        </ComboBox>

        <TextBox x:Name="txt1" Height="22" HorizontalAlignment="Left" Margin="8,257,0,0" TextWrapping="Wrap" Text="{Binding id, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" VerticalAlignment="Top" Width="120" Grid.Column="1" FontSize="14"/>

        <TextBox x:Name="txt2" Height="22" HorizontalAlignment="Left" Margin="141,257,0,0" TextWrapping="Wrap" Text="{Binding nameprod, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" VerticalAlignment="Top" Width="120" Grid.Column="1" FontSize="14"/>

        <TextBox x:Name="txt3" Height="22" HorizontalAlignment="Left" Margin="10,321,0,0" TextWrapping="Wrap" Text="{Binding nameprob, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" VerticalAlignment="Top" Width="120" Grid.Column="1" FontSize="14"/>

        <TextBox x:Name="txt4" HorizontalAlignment="Center" Height="22" Margin="0,130,0,0" TextWrapping="Wrap" Text="{Binding idnew, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding RelativeSource={RelativeSource Mode=Self}, Path = (Validation.Errors)[0].ErrorContent}" VerticalAlignment="Top" Width="86" FontSize="14"/>

        <TextBox x:Name="txt5" Height="22" HorizontalAlignment="Left" Margin="445,310,0,0" TextWrapping="Wrap" Text="{Binding type, UpdateSourceTrigger=PropertyChanged}" ToolTip ="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Mode=Self}}" VerticalAlignment="Top" Width="120" Grid.Column="1" FontSize="14"/>

        <Label x:Name="label_name" Content="Name" HorizontalAlignment="Left" Margin="184,231,0,0" VerticalAlignment="Top" Grid.Column="1" Width="44" Foreground="White"/>

        <Label x:Name="label_problem" Content="Problem" HorizontalAlignment="Left" Margin="43,292,0,0" VerticalAlignment="Top" Grid.Column="1" Width="54" Foreground="White"/>

        <Label x:Name="label_type_problem" Content="Type" HorizontalAlignment="Left" Margin="335,229,0,0" VerticalAlignment="Top" Grid.Column="1" Width="36" Foreground="White"/>

        <Label x:Name="label_state" Content="State" HorizontalAlignment="Left" Margin="188,292,0,0" VerticalAlignment="Top" Grid.Column="1" Width="36" Foreground="White"/>

        <Label x:Name="label_urgency" Content="Urgency" HorizontalAlignment="Left" Margin="335,292,0,0" VerticalAlignment="Top" Grid.Column="1" Width="54" Foreground="White"/>

        <Label x:Name="label_id" Content="ID" HorizontalAlignment="Center" Margin="0,101,0,0" VerticalAlignment="Top" Width="23" Foreground="White"/>

        <Label x:Name="label_id_product" Content="ID" HorizontalAlignment="Left" Margin="57,231,0,0" VerticalAlignment="Top" Grid.Column="1" Foreground="White" RenderTransformOrigin="0.763,0.509"/>

        <Label x:Name="label_filter" Content="Filter" HorizontalAlignment="Left" Margin="486,231,0,0" VerticalAlignment="Top" Grid.Column="1" Width="36" Foreground="White"/>

        <Label x:Name="label_filter_data" Content="Data" HorizontalAlignment="Left" Margin="488,284,0,0" VerticalAlignment="Top" Grid.Column="1" Width="34" Foreground="White"/>

        <Label x:Name="main_label" Content="Edit announcment" HorizontalAlignment="Center" Margin="0,24,0,0" VerticalAlignment="Top" Background="{x:Null}" Foreground="White" FontSize="20" Grid.Column="1"/>

    </Grid>
</Window>
